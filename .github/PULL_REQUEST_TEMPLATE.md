## Summary

## Checklist

- [ ] Syntax: all `.py` files parse
- [ ] Import: `from sw_2026_skill import SW` works
- [ ] No new `except: pass` without `traceback`
- [ ] No `time.sleep()` in `async def`
- [ ] File writes use `_validate_output_path()`
- [ ] markdownlint: zero warnings
